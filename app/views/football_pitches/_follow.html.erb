<%= form_for(current_user.follows.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :football_pitch_id, @football_pitch.id %></div>
  <%= f.submit t("follow.follow"), class: "btn btn-primary" %>
<% end %>
